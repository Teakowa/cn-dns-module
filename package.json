{
  "name": "cn-dns-module",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generate Surge [Host] module from dnsmasq domain lists",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "gen": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "typescript": "^5.9.3"
  }
}
